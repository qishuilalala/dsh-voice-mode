## 提交前检查

- [ ] 改动可追溯到需求；无顺手重构、无未要求的功能
- [ ] 行为类改动在 README / 注释中说明了语义影响（语音模式状态机、打断语义、设置分层）
- [ ] `npm run typecheck`（双 program）与 `npm test` 通过
- [ ] `node build.mjs` 后 `lib/` 产物与源码同步；bundle 用户记得重启提示
- [ ] 提交信息为中文，一行说明「为什么改」

> 插件以宿主权限运行；请勿引入来源不明的依赖或凭据。详见根目录 `BEST_PRACTICES.md`。
