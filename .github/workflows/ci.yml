name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: plugin/dsh-voice-mode
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22  # tests import src/*.ts directly (Node type stripping ≥22.18); runtime engines stays >=18
      - name: Install
        run: npm install --no-audit --no-fund
      # 注意：不跑 npm run typecheck——host 侧类型依赖 @deepseek-ai/dsh-* 宿主包，
      # 这些包未随目标版本发布到 npm（registry 仅 0.0.1-rc.1，本机用 dsh checkout
      # 的 0.1.1-rc.2）；CI 干净安装无法解析，且对陈旧类型面检查会误导。
      # typecheck 保持为本地门（需本机 dsh checkout 的 node_modules 提供真实宿主类型）。
      - name: Unit tests & manifest self-check
        run: npm test
      - name: Build
        run: npm run build
